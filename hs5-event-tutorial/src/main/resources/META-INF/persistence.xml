<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
             version="2.1">

    <persistence-unit name="SPATIAL-JPA">
        <description>
            Persistence unit for the JPA Hibernate Spatial Tutorial
        </description>


        <class>event.Event</class>
        <exclude-unlisted-classes>true</exclude-unlisted-classes>

        <properties>
            <property name="hibernate.dialect" value="org.hibernate.spatial.dialect.db2.DB2SpatialDialect"/>

            <!-- I prefer JPA properties whenever possible -->
            <property name="javax.persistence.jdbc.driver" value="com.ibm.db2.jcc.DB2Driver"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:db2://localhost:50000/hibern8"/>
            <property name="javax.persistence.jdbc.user" value="db2inst1"/>
            <property name="javax.persistence.jdbc.password" value="dbinst1-pwd"/>
            <property name="hibernate.connection.autocommit" value="true"/>


            <property name="hibernate.show_sql" value="true"/>

            <!-- format_sql property is disabled because we can also control via log configuration -->
            <!--<property name="hibernate.format_sql" value="true"/>-->

            <!-- enable this property to automatically create the database  -->
            <!--<property name="javax.persistence.schema-generation.database.action" value="drop-and-create"/>-->

        </properties>
    </persistence-unit>
</persistence>
